<UserControl x:Class="qltb.Views.ScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="20">
        <StackPanel>
            <Border BorderBrush="#1B5E20" BorderThickness="2" CornerRadius="10" Padding="20">
                <StackPanel>
                    <TextBlock Text="KẾT QUẢ QUÉT THIẾT BỊ" FontSize="16" Foreground="#1B5E20" FontWeight="Bold"/>
                    <Separator Margin="0,10"/>

                    <TextBlock Text="Tên vũ khí:" FontSize="12" Foreground="Gray"/>
                    <TextBlock Text="{Binding WeaponName}" FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>

                    <TextBlock Text="Tính năng kỹ chiến thuật:" FontSize="12" Foreground="Gray"/>
                    <Border Background="#F9F9F9" Padding="15" CornerRadius="5">
                        <TextBlock Text="{Binding Specifications}" TextWrapping="Wrap" FontSize="14" LineHeight="22"/>
                    </Border>

                    <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                        <TextBlock Text="Trạng thái: " VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Status}" Foreground="Green" FontWeight="Bold" FontSize="16"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>